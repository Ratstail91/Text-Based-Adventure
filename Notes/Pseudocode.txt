Parsing:

input

parse(string):
	//check each first level command
	sscanf(string, "%s", command)
	
	if (command == "look")
		return CommandLook(string + strlen("look"))
	
	...
end

-------------------------

Game State:
	Normal
	Quit
end